// Redesign 2026-09 (follow-up, round 22) — see app/agent/dashboard/
// layout.tsx's note: AgentCaptureScreen owns its own header now.
export default function AgentJobsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
